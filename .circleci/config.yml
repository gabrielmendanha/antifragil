# JavaScript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2.0

jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:13.7

    filters:
      branches:
        - only:
            - develop

    working_directory: ~/repo

    steps:
      - run:
          name: Compila projeto
          command: npm build --output-hashing=all

      - run:
          name: Instala dependências
          command: npm install

      - run:
          name: Limpa pasta do nginx
          command: ssh root@45.77.162.163 'rm -rf /root/www/*'

      - run:
          name: Copia compilado para o servidor
          command: scp -r dist/* root@45.77.162.163:/root/www/*

      - run:
          name: Copia compilado para o servidor
          command: ssh root@45.77.162.163 'rm -rf /root/www/*'
